import React from "react";
import { useAllStages } from "../../hooks/useAllStages";

function Map () {

    const { allStages } = useAllStages();

    return (
        <div>kikou</div>
    );

};
export default Map;