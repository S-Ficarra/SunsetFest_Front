export class StageModel {
    
    constructor (id, name, longitude, latitude, capacity) {
        this.id = id;
        this.name = name;
        this.longitude = longitude;
        this.latitude = latitude;
        this.capacity = capacity;
    };

};